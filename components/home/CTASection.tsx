export default function CTASection() {
  return (
    <section className="w-full bg-blue-600 text-white">
      <div className="max-w-7xl mx-auto px-4 py-12 sm:py-16 text-center">
        <h2 className="text-xl sm:text-3xl font-semibold">
          Looking for bulk orders?
        </h2>

        <p className="mt-2 text-sm sm:text-base">
          Get best price directly from us
        </p>

        <button className="mt-6 bg-white text-blue-600 px-5 sm:px-6 py-2.5 sm:py-3 rounded-lg font-medium hover:bg-gray-100 transition">
          Send Enquiry
        </button>
      </div>
    </section>
  );
}
